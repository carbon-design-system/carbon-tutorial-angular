version: 2.1
jobs:
  step-one:
    docker:
      - image: cimg/node:12.22.12-browsers
    working_directory: ~/repo
    steps:
      - checkout
      - run: npm install
      - run: npm run test:once
      - run: npm run build

workflows:
    main:
      jobs:
        - step-one
