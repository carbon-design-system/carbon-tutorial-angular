version: 2.1
jobs:
  step-one:
    docker:
      - image: circleci/node:14.17.0-browsers
    working_directory: ~/repo/carbon-angular-tutorial
    steps:
      - checkout:
          path: ~/repo
      - run: npm install
      - run: npm run test:once
      - run: npm run build

workflows:
    main:
      jobs:
        - step-one
