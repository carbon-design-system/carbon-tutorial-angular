import { NgModule, Component, Directive, ElementRef, Input } from "@angular/core";
import { getAttributes } from "@carbon/icon-helpers";
var QueryQueue20 = /** @class */ (function () {
    function QueryQueue20() {
        this.focusable = false;
    }
    QueryQueue20.decorators = [
        { type: Component, args: [{
                    selector: "ibm-icon-query-queue20",
                    template: "\n    <svg\n      ibmIconQueryQueue20\n      [ariaLabel]=\"ariaLabel\"\n      [ariaLabelledby]=\"ariaLabelledby\"\n      [ariaHidden]=\"ariaHidden\"\n      [title]=\"title\"\n      [isFocusable]=\"focusable\"\n      [attr.class]=\"innerClass\">\n    </svg>\n  "
                },] },
    ];
    QueryQueue20.propDecorators = {
        ariaLabel: [{ type: Input }],
        ariaLabelledby: [{ type: Input }],
        ariaHidden: [{ type: Input }],
        title: [{ type: Input }],
        focusable: [{ type: Input }],
        innerClass: [{ type: Input }]
    };
    return QueryQueue20;
}());
export { QueryQueue20 };
var QueryQueue20Directive = /** @class */ (function () {
    function QueryQueue20Directive(elementRef) {
        this.elementRef = elementRef;
        this.isFocusable = false;
    }
    QueryQueue20Directive.prototype.ngAfterViewInit = function () {
        var svg = this.elementRef.nativeElement;
        svg.setAttribute("xmlns", "http://www.w3.org/2000/svg");
        var domParser = new DOMParser();
        var rawSVG = "<svg focusable=\"false\" preserveAspectRatio=\"xMidYMid meet\" style=\"will-change: transform;\" xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 32 32\" aria-hidden=\"true\"><path d=\"M10 6h18v2H10zm0 6h18v2H10zm5 6h13v2H15zm-5 6h18v2H10zM4 14l7 5-7 5V14z\"></path></svg>";
        var svgElement = domParser.parseFromString(rawSVG, "image/svg+xml").documentElement;
        var node = svgElement.firstChild;
        while (node) {
            // importNode makes a clone of the node
            // this ensures we keep looping over the nodes in the parsed document
            svg.appendChild(svg.ownerDocument.importNode(node, true));
            node = node.nextSibling;
        }
        var attributes = getAttributes({
            width: 20,
            height: 20,
            viewBox: "0 0 32 32",
            title: this.title,
            "aria-label": this.ariaLabel,
            "aria-labelledby": this.ariaLabelledby,
            "aria-hidden": this.ariaHidden,
            focusable: this.isFocusable.toString()
        });
        var attrKeys = Object.keys(attributes);
        for (var i = 0; i < attrKeys.length; i++) {
            var key = attrKeys[i];
            var value = attributes[key];
            if (key === "title") {
                continue;
            }
            if (value) {
                svg.setAttribute(key, value);
            }
        }
        if (attributes.title) {
            var title = document.createElement("title");
            title.textContent = attributes.title;
            QueryQueue20Directive.titleIdCounter++;
            title.setAttribute("id", "query-queue20-" + QueryQueue20Directive.titleIdCounter);
            svg.appendChild(title);
            svg.setAttribute("aria-labelledby", "query-queue20-" + QueryQueue20Directive.titleIdCounter);
        }
    };
    QueryQueue20Directive.titleIdCounter = 0;
    QueryQueue20Directive.decorators = [
        { type: Directive, args: [{
                    selector: "[ibmIconQueryQueue20]"
                },] },
    ];
    /** @nocollapse */
    QueryQueue20Directive.ctorParameters = function () { return [
        { type: ElementRef }
    ]; };
    QueryQueue20Directive.propDecorators = {
        ariaLabel: [{ type: Input }],
        ariaLabelledby: [{ type: Input }],
        ariaHidden: [{ type: Input }],
        title: [{ type: Input }],
        isFocusable: [{ type: Input }]
    };
    return QueryQueue20Directive;
}());
export { QueryQueue20Directive };
var QueryQueue20Module = /** @class */ (function () {
    function QueryQueue20Module() {
    }
    QueryQueue20Module.decorators = [
        { type: NgModule, args: [{
                    declarations: [
                        QueryQueue20,
                        QueryQueue20Directive
                    ],
                    exports: [
                        QueryQueue20,
                        QueryQueue20Directive
                    ]
                },] },
    ];
    return QueryQueue20Module;
}());
export { QueryQueue20Module };
//# sourceMappingURL=20.js.map