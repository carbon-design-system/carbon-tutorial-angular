import { NgModule, Component, Directive, ElementRef, Input } from "@angular/core";
import { getAttributes } from "@carbon/icon-helpers";
var List32 = /** @class */ (function () {
    function List32() {
        this.focusable = false;
    }
    List32.decorators = [
        { type: Component, args: [{
                    selector: "ibm-icon-list32",
                    template: "\n    <svg\n      ibmIconList32\n      [ariaLabel]=\"ariaLabel\"\n      [ariaLabelledby]=\"ariaLabelledby\"\n      [ariaHidden]=\"ariaHidden\"\n      [title]=\"title\"\n      [isFocusable]=\"focusable\"\n      [attr.class]=\"innerClass\">\n    </svg>\n  "
                },] },
    ];
    List32.propDecorators = {
        ariaLabel: [{ type: Input }],
        ariaLabelledby: [{ type: Input }],
        ariaHidden: [{ type: Input }],
        title: [{ type: Input }],
        focusable: [{ type: Input }],
        innerClass: [{ type: Input }]
    };
    return List32;
}());
export { List32 };
var List32Directive = /** @class */ (function () {
    function List32Directive(elementRef) {
        this.elementRef = elementRef;
        this.isFocusable = false;
    }
    List32Directive.prototype.ngAfterViewInit = function () {
        var svg = this.elementRef.nativeElement;
        svg.setAttribute("xmlns", "http://www.w3.org/2000/svg");
        var domParser = new DOMParser();
        var rawSVG = "<svg focusable=\"false\" preserveAspectRatio=\"xMidYMid meet\" style=\"will-change: transform;\" xmlns=\"http://www.w3.org/2000/svg\" width=\"32\" height=\"32\" viewBox=\"0 0 32 32\" aria-hidden=\"true\"><path d=\"M10 6h18v2H10zm0 18h18v2H10zm0-9h18v2H10zm-6 0h2v2H4zm0-9h2v2H4zm0 18h2v2H4z\"></path></svg>";
        var svgElement = domParser.parseFromString(rawSVG, "image/svg+xml").documentElement;
        var node = svgElement.firstChild;
        while (node) {
            // importNode makes a clone of the node
            // this ensures we keep looping over the nodes in the parsed document
            svg.appendChild(svg.ownerDocument.importNode(node, true));
            node = node.nextSibling;
        }
        var attributes = getAttributes({
            width: 32,
            height: 32,
            viewBox: "0 0 32 32",
            title: this.title,
            "aria-label": this.ariaLabel,
            "aria-labelledby": this.ariaLabelledby,
            "aria-hidden": this.ariaHidden,
            focusable: this.isFocusable.toString()
        });
        var attrKeys = Object.keys(attributes);
        for (var i = 0; i < attrKeys.length; i++) {
            var key = attrKeys[i];
            var value = attributes[key];
            if (key === "title") {
                continue;
            }
            if (value) {
                svg.setAttribute(key, value);
            }
        }
        if (attributes.title) {
            var title = document.createElement("title");
            title.textContent = attributes.title;
            List32Directive.titleIdCounter++;
            title.setAttribute("id", "list32-" + List32Directive.titleIdCounter);
            svg.appendChild(title);
            svg.setAttribute("aria-labelledby", "list32-" + List32Directive.titleIdCounter);
        }
    };
    List32Directive.titleIdCounter = 0;
    List32Directive.decorators = [
        { type: Directive, args: [{
                    selector: "[ibmIconList32]"
                },] },
    ];
    /** @nocollapse */
    List32Directive.ctorParameters = function () { return [
        { type: ElementRef }
    ]; };
    List32Directive.propDecorators = {
        ariaLabel: [{ type: Input }],
        ariaLabelledby: [{ type: Input }],
        ariaHidden: [{ type: Input }],
        title: [{ type: Input }],
        isFocusable: [{ type: Input }]
    };
    return List32Directive;
}());
export { List32Directive };
var List32Module = /** @class */ (function () {
    function List32Module() {
    }
    List32Module.decorators = [
        { type: NgModule, args: [{
                    declarations: [
                        List32,
                        List32Directive
                    ],
                    exports: [
                        List32,
                        List32Directive
                    ]
                },] },
    ];
    return List32Module;
}());
export { List32Module };
//# sourceMappingURL=32.js.map